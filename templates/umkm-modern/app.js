const bind=(selector,value)=>{document.querySelectorAll(selector).forEach(el=>el.textContent=value??'')};document.querySelectorAll('[data-bind]').forEach(el=>{const path=el.dataset.bind.split('.');let value=SITE_DATA;path.forEach(k=>value=value?.[k]);el.textContent=value??''});document.querySelectorAll('[data-bind-img]').forEach(el=>{const path=el.dataset.bindImg.split('.');let value=SITE_DATA;path.forEach(k=>value=value?.[k]);el.src=value||''});const products=document.getElementById('product-list');SITE_DATA.products.forEach(p=>{products.insertAdjacentHTML('beforeend',`<article class="product-card"><div class="product-image-wrap"><img class="product-image" src="${p.image}" alt="${p.name}"></div><div class="product-body"><h3>${p.name}</h3><p>${p.description}</p><div class="price">${p.price}</div></div></article>`)});const gallery=document.getElementById('gallery-list');SITE_DATA.gallery.forEach((src,i)=>gallery.insertAdjacentHTML('beforeend',`<div class="gallery-item"><img src="${src}" alt="Galeri ${i+1}"></div>`));const testimonials=document.getElementById('testimonial-list');SITE_DATA.testimonials.forEach(t=>testimonials.insertAdjacentHTML('beforeend',`<article class="testimonial"><p>“${t.text}”</p><strong>${t.name}</strong></article>`));document.querySelectorAll('[data-whatsapp]').forEach(a=>{a.href=`https://wa.me/${SITE_DATA.business.whatsapp}?text=${encodeURIComponent('Halo '+SITE_DATA.business.name+', saya ingin bertanya tentang produk Anda.')}`});document.getElementById('year').textContent=new Date().getFullYear();
